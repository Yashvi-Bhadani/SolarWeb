[build]
builder = "nixpacks"
buildCommand = "npm ci --prefix frontend && npm ci --prefix backend && npm run build --prefix frontend"

[deploy]
startCommand = "node backend/server.js"
healthcheckPath = "/api/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
